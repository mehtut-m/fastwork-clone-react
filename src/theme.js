import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  status: {},
  palette: {
    primary: { main: '#7900FF' },
    // primary: { main: '#7900FF' },
  },
});

export default theme;
